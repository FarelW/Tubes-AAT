services:
  # ===========================================
  # REPORTING WRITE DATABASE (CQRS Command Side)
  # ===========================================
  reporting-write-db:
    image: postgres:15-alpine
    container_name: reporting-write-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: reporting_write_db
    volumes:
      - reporting-write-db-data:/var/lib/postgresql/data
      - ./scripts/init-reporting-write-db.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5436:5432"
    networks:
      - poc-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ===========================================
  # REPORTING READ DATABASE (CQRS Query Side)
  # ===========================================
  reporting-read-db:
    image: postgres:15-alpine
    container_name: reporting-read-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: reporting_read_db
    volumes:
      - reporting-read-db-data:/var/lib/postgresql/data
      - ./scripts/init-reporting-read-db.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5435:5432"
    networks:
      - poc-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5


  # ===========================================
  # OPERATIONS DATABASE
  # ===========================================
  operations-db:
    image: postgres:15-alpine
    container_name: operations-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: operations_db
    volumes:
      - operations-db-data:/var/lib/postgresql/data
      - ./scripts/init-operations-db.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5433:5432"
    networks:
      - poc-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ===========================================
  # WORKFLOW DATABASE
  # ===========================================
  workflow-db:
    image: postgres:15-alpine
    container_name: workflow-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: workflow_db
    volumes:
      - workflow-db-data:/var/lib/postgresql/data
      - ./scripts/init-workflow-db.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5434:5432"
    networks:
      - poc-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ===========================================
  # REDIS (Event Bus)
  # ===========================================
  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - poc-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ===========================================
  # REPORTING SERVICE (Citizen-Facing) - CQRS Enabled
  # ===========================================
  reporting-service:
    build:
      context: .
      dockerfile: cmd/reporting-service/Dockerfile
    container_name: reporting-service
    environment:
      # Write DB (Command Side)
      - WRITE_DB_HOST=reporting-write-db
      - WRITE_DB_PORT=5432
      - WRITE_DB_USER=postgres
      - WRITE_DB_PASSWORD=postgres
      - WRITE_DB_NAME=reporting_write_db
      # Read DB (Query Side)
      - READ_DB_HOST=reporting-read-db
      - READ_DB_PORT=5432
      - READ_DB_USER=postgres
      - READ_DB_PASSWORD=postgres
      - READ_DB_NAME=reporting_read_db
      # Event Bus
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - SERVER_PORT=8080
      - INSTANCE_ID=reporting-1
    ports:
      - "8080:8080"
    depends_on:
      reporting-write-db:
        condition: service_healthy
      reporting-read-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - poc-network
    restart: unless-stopped


  # ===========================================
  # OPERATIONS SERVICE (Officer-Facing)
  # ===========================================
  operations-service:
    build:
      context: .
      dockerfile: cmd/operations-service/Dockerfile
    container_name: operations-service
    environment:
      - DB_HOST=operations-db
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=operations_db
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - SERVER_PORT=8081
      - INSTANCE_ID=operations-1
    ports:
      - "8081:8081"
    depends_on:
      operations-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - poc-network
    restart: unless-stopped

  # ===========================================
  # WORKFLOW SERVICE (Async/SLA/Notifications)
  # ===========================================
  workflow-service:
    build:
      context: .
      dockerfile: cmd/workflow-service/Dockerfile
    container_name: workflow-service
    environment:
      - DB_HOST=workflow-db
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=workflow_db
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - SERVER_PORT=8082
      - INSTANCE_ID=workflow-1
    ports:
      - "8082:8082"
    depends_on:
      workflow-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - poc-network
    restart: unless-stopped

  # ===========================================
  # FRONTEND (React + Nginx)
  # ===========================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - "3000:80"
    depends_on:
      - reporting-service
      - operations-service
      - workflow-service
    networks:
      - poc-network
    restart: unless-stopped

networks:
  poc-network:
    driver: bridge

volumes:
  reporting-write-db-data:
  reporting-read-db-data:
  operations-db-data:
  workflow-db-data:
  redis-data:
